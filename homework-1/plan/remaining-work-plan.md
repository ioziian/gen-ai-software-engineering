# Homework 1 Remaining Work Plan

## Summary
The core banking transactions API is already implemented. This plan covers only the remaining or inconsistent pieces that still need attention so the submission matches the assignment deliverables cleanly.

## Remaining Work
- Fix documentation mismatches in `HOWTORUN.md` and `README.md`.
- Add the missing `demo/sample-requests.http` file if it is still referenced in docs.
- Confirm the required screenshots exist in `docs/screenshots/`:
  - AI tool interaction screenshots
  - API running successfully
  - sample request/response screenshots
- Verify demo file naming is consistent across the repo.
- Review the implementation for any small spec gaps that may still be present, especially around validation and filtering behavior.

## Assumptions
- The API endpoints, validation, filtering, and summary feature are treated as already implemented.
- This plan is intended to guide only the unfinished submission tasks, not rewrite completed code.
